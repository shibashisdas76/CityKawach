{
  "name": "citykawach-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --cwd apps/registry-web dev",
    "build": "bun --cwd apps/registry-web build",
    "preview": "bun --cwd apps/registry-web preview",
    "typecheck": "bun --cwd apps/registry-web typecheck"
  }
}
